# Quotex Trading Bot

This project is a trading bot for Quotex.

## Setup

1. Install dependencies:
   ```bash
   pip install -r requirements.txt
   ```

2. Configure credentials in `user_config.py` (not included in repo).
